{
  "name": "node-dev",
  "version": "0.0.1",
  "dependencies": {
    "express": "*",
    "underscore": "*"
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "gulp test",
    "start": "node server.js",
    "docker": "npm install && forever --watch --watchDirectory /app /app/server.js"
  }
}
